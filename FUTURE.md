# Future Features

Nice-to-haves that were intentionally skipped to keep the MVP tight.

- **List expiry** — automatically delete lists older than 30 days with no activity
- **Item counts badge** — show unchecked item count in the page title and recent lists
- **Bulk clear checked** — one-tap to delete all checked items
- **Custom list emoji** — let users pick an emoji icon for their list
- **QR code share** — generate a QR code for the room code on the share sheet
